name: Keep-warm ping

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping healthz
        run: curl -fsS --max-time 10 "${{ secrets.PRODUCTION_URL }}/api/healthz"
